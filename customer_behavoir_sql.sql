create database customer_behavior;
select * from customer;



#Q1. What is the total revenue generated by male vs. female customers?
 select gender,sum(purchase_amount) as "total_revenue" from customer group by gender;
 
#-Q2. Which customers used a discount but still spent more than the average purchase amount?
select customer_id , purchase_amount from customer where discount_applied = "yes" and purchase_amount > (select avg(purchase_amount) from customer);

#Q3. Which are the top 5 products with the highest average review rating?
select item_purchased,round(avg(review_rating),2) product from customer group by item_purchased order by product desc limit 5 ;

#Q4. Compare the average Purchase Amounts between Standard and Express Shipping.
select shipping_type,avg(purchase_amount) as average_purchase from customer where shipping_type = "express" or shipping_type = "standard" group by shipping_type; 
/*Q5. Do subscribed customers spend more? Compare average spend and total revenue 
--between subscribers and non-subscribers*/
select subscription_status,sum(purchase_amount) as total_revenue , avg(purchase_amount) average_spend from customer group by subscription_status ;
#Q6 Which 5 products have the highest percentage of purchases with discounts applied?
select item_purchased,  ROUND(100.0 * SUM(CASE WHEN discount_applied = 'Yes' THEN 1 ELSE 0 END)/COUNT(*),2) as discount_rate from customer
group by item_purchased
order by discount_rate desc limit 5;
#Q7. Segment customers into New, Returning, and Loyal based on their total
 with customer_type as (
SELECT customer_id, previous_purchases,
CASE 
    WHEN previous_purchases = 1 THEN 'New'
    WHEN previous_purchases BETWEEN 2 AND 10 THEN 'Returning'
    ELSE 'Loyal'
    END AS customer_segment
FROM customer)

select customer_segment,count(*) AS "Number of Customers" 
from customer_type 
group by customer_segment;
#Q8. What are the top 3 most purchased products within each category?
WITH item_counts AS (
select category,item_purchased,count(customer_id) as "total_orders" , ROW_NUMBER() OVER (PARTITION BY category
 ORDER BY COUNT(customer_id) DESC) AS item_rank from customer   group by category , item_purchased ) 
 SELECT item_rank,category, item_purchased, total_orders
FROM item_counts
WHERE item_rank <=3;
#Q9. Are customers who are repeat buyers (more than 5 previous purchases) also likely to subscribe?
select subscription_status,count(subscription_status) as "subcribe" from  customer where previous_purchases > 5  group by subscription_status;
#10. What is the revenue contribution of each age group? 
select agegroup,sum(purchase_amount) as total_revenue from customer group by agegroup order by total_revenue desc;

